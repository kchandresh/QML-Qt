import QtQuick 2.15
import QtQuick.Window 2.15

Rectangle{
    id: leftrect
    anchors { top: ...}
    color: focus ? "red" : "darkred"
    KeyNavigation.right: righrect
    focus: true
}
Rectangle{
    id : rightrects
    anchors{ top: ...}
        color: focus ? "red" : "darkred"
        KeyNavigation.
}
